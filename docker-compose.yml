services:
  api:
    build:
      context: .
      dockerfile: packages/api/Dockerfile
    container_name: sfa-api
    restart: unless-stopped
    ports:
      - "4000:4000"
    env_file:
      - .env
    environment:
      PORT: 4000
    command: >
      sh -c "node packages/api/dist/seed/seed.js && node packages/api/dist/main.js"
